# V1.5 P2与概要隔离的质量门实施计划

<!-- language-switch -->
[English](../../../en/superpowers/plans/2026-07-19-v1.5-p2-quality-gate-implementation.md)





> **中文说明：** 本实施计划构建可过滤的全配置索引、确定性的组合融合、绑定索引修订版的 RAG 指标，以及强制执行的默认配置质量门禁。

b> **对于代理工作者：**需要的子技能：使用$superpower-subagents（推荐）或$superpower- execution -plans逐个任务地执行此计划。步骤使用复选框（' -[]'）语法通过update_plan进行跟踪。

**目标：**建立一个可过滤的所有配置文件索引，确定性的组合融合，修订绑定的RAG质量指标，以及强制的默认配置文件门。

**架构：**每次摄取都创建一个可重用的超集，包括原始、父、子和两个QA入口组。新的Milvus v2集合在排序前过滤候选集合，而遗留集合在知识库完成迁移之前仍然是可读的。评估存储当前修订的度量和gate决策；非经典的默认更改需要当前的‘ PASSED ’决定。

**技术栈：** Spring Boot/JPA/Flyway/Jackson， Milvus Java SDK和pymilvus， Python/httpx/rank-bm25, React/Vite, JUnit/Mockito, pytest, Vitest。

---

任务0：检查点验证P0/P1基线

* *文件:* *
现有未提交的P0/P1文件由git status报告
-排除：本P2计划和已提交的P2设计规范

-[] **步骤1：分级前检查范围

运行:

```powershell

git -c safe.directory='D:/software/cld_project/dupi-RAG/.worktrees/v1.5-rag-quality-upgrade' status --short
git -c safe.directory='D:/software/cld_project/dupi-RAG/.worktrees/v1.5-rag-quality-upgrade' diff --check

```

预期：只有已知的P0/P1实现和文档文件是脏的；没有列出缓存或构建输出。

-[] **步骤2：重新依次运行验证过的P0/P1门

使用' $env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m‘ ’运行API：

```powershell

mvn "-Dmaven.repo.local=.m2/repository" -f services/api/pom.xml "-Dtest=KnowledgeBaseServiceTest,RetrievalServiceTest,RagEvalServiceTest,IngestJobProducerTest,IngestJobServiceTest,ControllerLayerTest,QaGenerationServiceTest,ConfigAndExceptionTest" test

```

运行工作人员:

```powershell

pytest services/worker/tests/test_main.py services/worker/tests/test_qa_indexer.py services/worker/tests/test_canonical_parser_consumer.py services/worker/tests/test_parent_child_chunker.py services/worker/tests/test_embedder_hybrid.py services/worker/tests/test_indexer.py -q --basetemp .pytest-tmp/p2-baseline

```

从“services/ Web”运行Web：

```powershell

npm test -- KbDetailPage RagEvalPanel resources
npm run build

```

期望：API、Worker、Web和产品构建全部通过。

-[] **第三步：只提交经过验证的基线

执行已知的P0/P1路径，检查‘ git diff——cached——name-only ’，然后提交：

```text

feat(rag): add v1.5 profile indexing and qa retrieval

```

如果P2计划还没有提交，就不要执行它。

任务1:V10索引和评估状态模型

* *文件:* *
创建：“services/api/src/main/resources/db/migration/V19__profile_index_quality_gate”。sql '（在合并V17的主要迁移后重新编号）
-创建：‘ services/api/src/main/java/com/dupi/rag/domain/enums/RagEvalGateStatus.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/domain/entity/Document.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/domain/entity/KnowledgeBase.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/domain/entity/RagEvalRun.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/domain/entity/RagEvalRunResult.java ’
-修改：‘ services/api/src/main/java/ com/dupi/rag/repository/documentpository.java ’
-测试：‘ services/api/src/ Test /java/com/dupi/rag/config/ConfigAndExceptionTest.java ’

-[] **步骤1：写入失败的迁移/实体测试**

扩展模式/实体测试来断言这些映射和默认值：

```java

assertThat(Document.builder().build().getIndexSchemaVersion()).isEqualTo(1);
assertThat(KnowledgeBase.builder().build().getIndexRevision()).isZero();
assertThat(RagEvalRun.builder().build().getGateSummary()).isEmpty();
assertThat(RagEvalRunResult.builder().build().isHitPassed()).isFalse();
assertThat(RagEvalRunResult.builder().build().isCitationEligible()).isFalse();
assertThat(RagEvalRunResult.builder().build().isCitationPassed()).isFalse();

```

-[] **步骤2：执行测试，验证RED**

```powershell

mvn "-Dmaven.repo.local=.m2/repository" -f services/api/pom.xml "-Dtest=ConfigAndExceptionTest" test

```

预期：编译失败，因为新的实体字段不存在。

-[] **步骤3：添加V10和实体字段**

迁移必须包含设计所需的持久状态：

```sql

ALTER TABLE documents ADD COLUMN index_schema_version INTEGER NOT NULL DEFAULT 1;
ALTER TABLE knowledge_bases ADD COLUMN index_revision BIGINT NOT NULL DEFAULT 0;
ALTER TABLE rag_eval_runs ADD COLUMN index_revision BIGINT;
ALTER TABLE rag_eval_runs ADD COLUMN gate_summary JSONB NOT NULL DEFAULT '{}'::jsonb;
ALTER TABLE rag_eval_run_results ADD COLUMN hit_passed BOOLEAN NOT NULL DEFAULT FALSE;
ALTER TABLE rag_eval_run_results ADD COLUMN citation_eligible BOOLEAN NOT NULL DEFAULT FALSE;
ALTER TABLE rag_eval_run_results ADD COLUMN citation_passed BOOLEAN NOT NULL DEFAULT FALSE;
UPDATE vector_cleanup_tasks SET target_type = 'LEGACY_KNOWLEDGE_BASE' WHERE target_type = 'KNOWLEDGE_BASE';
UPDATE vector_cleanup_tasks SET target_type = 'LEGACY_DOCUMENT' WHERE target_type = 'DOCUMENT';

```

添加匹配的生成器默认值。添加存储库方法：

```java

long countByKbId(UUID kbId);
long countByKbIdAndIndexSchemaVersionLessThan(UUID kbId, int version);

```

定义门状态“PASSED”、“BLOCKED”、“INSUFFICIENT_DATA”、“STALE”、“INDEX_NOT_READY”和“not_evaluate”。

-[] **步骤4：验证GREEN并提交**

运行‘ ConfigAndExceptionTest ’，然后提交：

```text

feat(rag): add profile index and gate state model

```

任务2：通用工作者块计划

* *文件:* *
-创建：‘ services/worker/app/profile_index_plan.py ’
创建：‘ services/worker/tests/test_profile_index_plan.py ’
修改：‘ services/worker/app/qa_index .py ’
修改：“services/worker/app/consumer.py”
修改：“services/worker/app/main.py”
修改：“services/worker/app/models.py”
-修改：‘ services/worker/tests/test_qa_index .py ’
-修改：‘ services/worker/tests/test_canonical_parser_consumer.py ’

-[] **步骤1：编写失败的通用计划测试**

创建一个测试，构建一个计划并断言：

```python

assert roles(plan.persisted_chunks) == ["original", "parent", "child", "qa", "qa"]
assert scope(original) == ["classic", "qa-assisted"]
assert scope(parent) == ["parent-child", "combined"]
assert scope(child) == ["parent-child", "combined"]
assert scope(original_qa) == ["qa-assisted"]
assert scope(parent_qa) == ["combined"]
assert plan.legacy_chunks == [original]
assert [chunk.chunk_index for chunk in plan.persisted_chunks] == list(range(5))

```

还要断言两个独立的QA失败仍然返回原始、父和子条目。

-[] **步骤2：运行测试以验证RED**

```powershell

pytest services/worker/tests/test_profile_index_plan.py services/worker/tests/test_qa_indexer.py -q --basetemp .pytest-tmp/p2-plan-red

```

预期：导入‘ profile_index_plan ’失败。

-[] **步骤3：实现块计划边界**

创建:

```python

@dataclass
class ProfileIndexPlan:
    persisted_chunks: list[TextChunk]
    v2_index_chunks: list[TextChunk]
    legacy_chunks: list[TextChunk]

def build_profile_index_plan(nodes, chunk_size, chunk_overlap, strategy, embed_fn,
                             kb_id, doc_id) -> ProfileIndexPlan:
    original_chunks = chunk_nodes(
        nodes, chunk_size=chunk_size, chunk_overlap=chunk_overlap,
        strategy=strategy, embed_fn=embed_fn,
    )
    parent_chunks, child_chunks = build_parent_child_chunks(
        nodes, child_chunk_size=chunk_size, child_overlap=chunk_overlap,
        strategy=strategy, embed_fn=embed_fn,
    )
    mark_scope(original_chunks, "original", ["classic", "qa-assisted"])
    mark_scope(parent_chunks, "parent", ["parent-child", "combined"])
    mark_scope(child_chunks, "child", ["parent-child", "combined"])
    original_qa = generate_qa_chunks(
        kb_id, doc_id, original_chunks, start_index=0,
        profile_scope=["qa-assisted"],
    )
    parent_qa = generate_qa_chunks(
        kb_id, doc_id, parent_chunks, start_index=0,
        profile_scope=["combined"],
    )
    persisted = [*original_chunks, *parent_chunks, *child_chunks, *original_qa, *parent_qa]
    for index, chunk in enumerate(persisted):
        chunk.chunk_index = index
    return ProfileIndexPlan(
        persisted_chunks=persisted,
        v2_index_chunks=[*original_chunks, *child_chunks, *original_qa, *parent_qa],
        legacy_chunks=original_chunks,
    )

```

一次构建原始和父/子块。在QA生成之前设置‘ entry_kind ’和‘ profile_scope ’元数据。用显式的‘ profile_scope ’参数扩展‘ generate_qa_chunks ’，这样继承的源范围就不会泄漏到QA条目中。在组装完所有组后，连续重新分配每个持久化块索引。

修改‘ consumer.py ’，让它在每次摄取时都调用这个计划器，与所选的默认配置文件无关。在完成回调中包含‘ indexSchemaVersion=2 ’。扩展Redis标准化‘ legacywriterrequired ’和‘ indexSchemaVersion ’；默认‘ legacywriterrequired ’为‘ true ’，直到API开始在Task 4中发出该字段。

-[] **步骤4：验证GREEN并提交**

运行新的计划、QA和用户测试，然后提交：

```text

feat(worker): build reusable profile index entries

```

任务3:Worker V2 Milvus模式、过滤器和传统双写

* *文件:* *
修改：‘ services/worker/app/config.py ’
修改：‘ services/worker/app/index .py ’
修改：“services/worker/app/consumer.py”
修改：‘ services/worker/tests/test_index .py ’
-修改：‘ services/worker/tests/test_canonical_parser_consumer.py ’
修改：“deploy/.env.example”

-[] **步骤1：编写失败的概要索引测试**

测试v2的创建包括：

```python

expected_fields = {
    "chunk_id", "kb_id", "doc_id", "content", "entry_kind",
    "profile_classic", "profile_parent_child",
    "profile_qa_assisted", "profile_combined", "embedding",
}

```

测试插入从‘ profile_scope ’派生布尔值。Test ‘ search_profile ’接受一个规范化的配置文件和可选的白名单‘ entry_kind ’，生成如下表达式：

```text

kb_id == "<validated-kb-uuid>" and profile_combined == true and entry_kind == "qa"

```

测试任意配置文件/条目字符串被拒绝。只有当‘ legacywriterrequired =true ’时，测试消费者才会将原始块写入遗留集合。

-[] **步骤2：运行测试以验证RED**

```powershell

pytest services/worker/tests/test_indexer.py services/worker/tests/test_canonical_parser_consumer.py -q --basetemp .pytest-tmp/p2-index-red

```

-[] **步骤3：实现v2索引支持**

添加设置:

```python

milvus_profile_collection = os.getenv(
    "MILVUS_PROFILE_COLLECTION",
    os.getenv("MILVUS_COLLECTION", "dupi_chunks") + "_profiles_v2",
)
combined_child_weight = float(os.getenv("COMBINED_CHILD_WEIGHT", "1.0"))
combined_qa_weight = float(os.getenv("COMBINED_QA_WEIGHT", "0.8"))
rrf_k = int(os.getenv("RRF_K", "60"))

```

使用显式的集合名称和模式模式扩展‘ MilvusIndexer ’。添加‘ index_profile_chunks ’和‘ search_profile ’；为基集合保留遗留方法。用户总是在v2、索引计划中替换文档。V2_index_chunks ‘，可选地替换/indexes ’计划。legacy_chunks’，并在回调中报告v2块id。

-[] **步骤4：验证GREEN并提交**

运行Worker索引器和消费者测试，然后提交：

```text

feat(worker): add filtered v2 profile collection

```

任务4:API索引准备、修订和遗留清理

* *文件:* *
-创建：‘ services/api/src/main/java/com/dupi/rag/service/ProfileIndexStateService.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/dto/IngestJobMessage.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/dto/IngestStatusUpdate.java ’
修改：‘ services/api/src/main/java/com/dupi/rag/service/IngestJobProducer.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/service/IngestJobService.java ’
修改：‘ services/api/src/main/java/com/dupi/rag/service/DocumentService.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/service/KnowledgeBaseService.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/repository/KnowledgeBaseRepository.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/domain/enums/VectorCleanupTargetType.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/service/VectorCleanupTaskService.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/client/MilvusVectorService.java ’
-测试：‘ services/api/src/ Test /java/com/dupi/rag/service/IngestJobProducerTest.java ’
-测试：‘ services/api/src/ Test /java/com/dupi/rag/service/IngestJobServiceTest.java ’
-测试：‘ services/api/src/ Test /java/com/dupi/rag/service/DocumentServiceTest.java ’
-测试：‘ services/api/src/ Test /java/com/dupi/rag/service/VectorCleanupTaskServiceTest.java ’

-[] **步骤1：编写失败的生命周期测试

覆盖这些过渡：

```java

assertThat(state.isV2Ready(kbId)).isFalse(); // no docs or any v1 doc
assertThat(message.getLegacyWriteRequired()).isTrue();
assertThat(message.getIndexSchemaVersion()).isEqualTo(2);

```

在完成回调时：保存块，标记文档v2，增量KB修订，并仅在最后一个活动文档变为v2时排队进行遗留KB清理。上传、删除和重新启动每个无效版本。Reindex重置文档架构版本，只删除v2向量，不删除遗留向量。

-[] **步骤2：运行重点生命周期测试以验证RED**

```powershell

mvn "-Dmaven.repo.local=.m2/repository" -f services/api/pom.xml "-Dtest=IngestJobProducerTest,IngestJobServiceTest,DocumentServiceTest,VectorCleanupTaskServiceTest" test

```

-[] **步骤3：实现状态转换和清理目标**

“ProfileIndexStateService”公开:

```java

boolean isV2Ready(UUID kbId);
long currentRevision(UUID kbId);
void bumpRevision(KnowledgeBase kb);
void resetForReindex(KnowledgeBase kb, List<Document> documents);

```

‘ IngestJobProducer ’设置' legacywriterrequired =！isV2Ready(kbId) '和目标模式版本2。用四个明确的值替换清理目标值：‘ PROFILE_KNOWLEDGE_BASE ’， ‘ PROFILE_DOCUMENT ’， ‘ LEGACY_KNOWLEDGE_BASE ’和‘ LEGACY_DOCUMENT ’。V10将旧任务转换为遗留目标。文档和知识库的删除使概要文件和遗留目标同时排队；驯鹿启动只排队/删除配置文件目标；成功的切换只排队等候‘ LEGACY_KNOWLEDGE_BASE ’。“VectorCleanupTaskService。Process将每个枚举分配给特定于集合的严格删除方法。切换遗留删除失败仍然等待正常重试/回退。

-[] **步骤4：验证GREEN并提交**

运行四个生命周期套件，然后提交：

```text

feat(api): track profile index readiness and revision

```

任务5:API V2 Milvus搜索和组合矢量融合

* *文件:* *
-创建：‘ services/api/src/main/java/ com/dupi/rag/service/weightedrrfffusion .java ’
-创建：‘ services/api/src/main/java/com/dupi/rag/exception/RetrievalProfileConflictException.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/config/MilvusProperties.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/config/RagProperties.java ’
修改：“services/api/src/main/resources/application.yml”
-修改：‘ services/api/src/main/java/com/dupi/rag/client/MilvusVectorService.java ’
修改：“services/api/src/main/java/com/dupi/rag/service/RetrievalService.java”
-测试：‘ services/api/src/ Test /java/com/dupi/rag/client/MilvusVectorServiceTest.java ’
-测试：‘ services/api/src/ Test /java/com/dupi/rag/service/RetrievalServiceTest.java ’

-[] **步骤1：编写失败的模式/过滤器/融合测试**

断言API创建或验证与Worker相同的v2标量字段。断言配置文件表达式仅来自‘ RetrievalProfile ’和入口类常量。测试加权RRF：

```java

var fused = fusion.fuse(List.of(
    new Route(1.0, List.of(childA, childB)),
    new Route(0.8, List.of(qaA))
), 60);
assertThat(fused).extracting(ScoredHit::id).containsExactly("child-a", "qa-a", "child-b");

```

Test ready classic使用v2 classic过滤器，not-ready classic使用legacy， not-ready non-classic引发冲突。Test Combined分别查询子节点和QA，在诊断中包含权重，接受空路由，扩展上下文，并删除最终id。

-[] **步骤2：运行重点测试以验证RED**

```powershell

mvn "-Dmaven.repo.local=.m2/repository" -f services/api/pom.xml "-Dtest=MilvusVectorServiceTest,RetrievalServiceTest" test

```

-[] **步骤3：实现安全过滤搜索和融合**

添加‘ profileCollection ’， getter返回到‘ collection + "_profiles_v2" ’。添加有效的默认值到‘ RagProperties ’。公开:

```java

List<SearchResult> searchLegacy(UUID kbId, List<Float> vector, int topK);
List<SearchResult> searchProfile(UUID kbId, List<Float> vector, int topK,
                                 RetrievalProfile profile, String entryKind);

```

‘ RetrievalService ’从readiness中选择legacy/v2，对Combined执行两次过滤搜索，与‘ WeightedRrfFusion ’融合，并在评分前通过‘ profile_scope ’和‘ entry_kind ’过滤本地回退块。诊断包括‘ combinedChildWeight ’、‘ combinedQaWeight ’、‘ rrfK ’和路由命中计数。

在此任务中创建‘ RetrievalProfileConflictException ’，以便独立编译未准备好的非经典检索。它携带一个稳定的原因码和消息；HTTP映射是在Task 8中添加的。

-[] **步骤4：验证GREEN并提交**

运行两个集中的套件，然后提交：

```text

feat(retrieval): isolate profiles and fuse combined routes

```

任务6：工人混合配置文件过滤和组合融合

* *文件:* *
修改：“services/worker/app/main.py”
修改：“services/worker/app/retrieval/hybrid.py”
-修改：‘ services/worker/tests/test_embedder_hybrid.py ’
修改：“services/api/src/main/java/com/dupi/rag/service/RetrievalService.java”
-测试：‘ services/api/src/ Test /java/com/dupi/rag/service/RetrievalServiceTest.java ’

-[] **步骤1：写入失败的混合路由测试例**

测试经典/亲子/ qa辅助语料库过滤。对于Combined，提供独立的子向量和QA向量/BM25结果，并断言加权RRF排序。断言空QA路由保留子结果，并在融合后重新排序执行。Assert not-ready classic使用遗留搜索。

-[] **步骤2：运行测试以验证RED**

```powershell

pytest services/worker/tests/test_embedder_hybrid.py -q --basetemp .pytest-tmp/p2-hybrid-red
mvn "-Dmaven.repo.local=.m2/repository" -f services/api/pom.xml "-Dtest=RetrievalServiceTest" test

```

-[] **第三步：实现请求和路由混合

将‘ profile_index_ready ’添加到‘ hybridretriieverrequest ’和API请求正文中。评分前对BM25语料进行过滤。使用一般化的帮助器：

```python

def weighted_rrf(routes: list[tuple[float, list[dict]]], k: int = 60) -> list[dict]:
    if k < 1:
        raise ValueError("RRF K must be at least 1")
    scores: dict[str, float] = {}
    items: dict[str, dict] = {}
    for weight, hits in routes:
        if weight <= 0:
            raise ValueError("route weight must be positive")
        for rank, hit in enumerate(hits):
            chunk_id = hit["chunk_id"]
            scores[chunk_id] = scores.get(chunk_id, 0.0) + weight / (k + rank + 1)
            items[chunk_id] = hit
    return [
        {**items[chunk_id], "score": score}
        for chunk_id, score in sorted(scores.items(), key=lambda item: item[1], reverse=True)
    ]

```

非组合剖面在其孤立的语料库上保留了现有的矢量/BM25 RRF行为。组合式构建四条路由，并在可选的重新排名之前应用子/QA权重。

-[] **步骤4：验证GREEN并提交**

运行Worker混合测试和API检索测试，然后提交：

```text

feat(retrieval): add weighted combined hybrid fusion

```

任务7：修订绑定评估度量和门总结

**Files:**
- Create: `services/api/src/main/java/com/dupi/rag/dto/RagEvalProfileMetricsResponse.java`
- Create: `services/api/src/main/java/com/dupi/rag/dto/RagEvalGateDecisionResponse.java`
- Create: `services/api/src/main/java/com/dupi/rag/service/RetrievalProfileGateService.java`
- Modify: `services/api/src/main/java/com/dupi/rag/dto/RagEvalRunResponse.java`
- Modify: `services/api/src/main/java/com/dupi/rag/dto/RagEvalRunResultResponse.java`
- Modify: `services/api/src/main/java/com/dupi/rag/repository/RagEvalRunRepository.java`
- Modify: `services/api/src/main/java/com/dupi/rag/service/RagEvalService.java`
- Test: `services/api/src/test/java/com/dupi/rag/service/RagEvalServiceTest.java`
- Test: `services/api/src/test/java/com/dupi/rag/service/RetrievalProfileGateServiceTest.java`

-[] **步骤1：编写失败的度量和决策测试**

测试请求候选人自动包含经典。构建具有引用资格的三个经典和三个候选结果，并断言准确的比率/delta。涵盖每一个决定：

```java

assertStatus(passing, PASSED);
assertStatus(hitRegression, BLOCKED);
assertStatus(citationRegression, BLOCKED);
assertStatus(twoCasesOnly, INSUFFICIENT_DATA);
assertStatus(noCitationCases, INSUFFICIENT_DATA);
assertStatus(revisionChanged, STALE);
assertStatus(indexNotReady, INDEX_NOT_READY);

```

-[] **步骤2：运行测试以验证RED**

```powershell

mvn "-Dmaven.repo.local=.m2/repository" -f services/api/pom.xml "-Dtest=RagEvalServiceTest,RetrievalProfileGateServiceTest" test

```

-[] **第三步：实现计算和持久化**

‘ RetrievalProfileGateService ’从持久化的结果布尔值中计算带有倍数的指标，创建候选与经典决策，将它们序列化到‘ RagEvalRun ’中。gatessummary '，并返回键入的响应DTOs。‘ RagEvalService ’捕获开始修订，记录‘ hitPassed ’， ‘ citationEligible ’和‘ citationPassed ’，在执行后重新计算当前修订，然后存储‘ STALE ’决定，如果它改变了。

暴露这些方法：

```java

Map<RetrievalProfile, RagEvalGateDecisionResponse> calculate(
        List<RagEvalRunResult> results,
        long runRevision,
        long currentRevision,
        boolean indexReady
);
RagEvalGateDecisionResponse latestDecision(UUID kbId, RetrievalProfile candidate);
void assertCanActivate(UUID kbId, RetrievalProfile candidate);

```

每当请求任何非经典配置文件时，配置文件规范化程序都会在前面加上‘ CLASSIC ’。

-[] **步骤4：验证GREEN并提交**

运行两个gate套件，然后提交：

```text

feat(eval): add revision-bound retrieval quality gates

```

任务8：执行默认配置文件门和暴露准备

* *文件:* *
-修改：‘ services/api/src/main/java/com/dupi/rag/exception/RetrievalProfileConflictException.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/exception/GlobalExceptionHandler.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/dto/KnowledgeBaseResponse.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/service/KnowledgeBaseService.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/controller/KnowledgeBaseController.java ’
-修改：‘ services/api/src/main/java/com/dupi/rag/service/KnowledgeBaseExportService.java ’
-测试：‘ services/api/src/ Test /java/com/dupi/rag/service/KnowledgeBaseServiceTest.java ’
-测试：‘ services/api/src/ Test /java/com/dupi/rag/controller/ControllerLayerTest.java ’
-测试：‘ services/api/src/ Test /java/com/dupi/rag/config/ConfigAndExceptionTest.java ’

-[] **步骤1：编写失败的强制测试**

断言新的知识库拒绝非经典缺省值。断言切换到经典总是有效的。断言每个非经典拒绝映射到HTTP 409，并使用稳定代码‘ retrieval_profile_gate_blocked ’和决策原因。Assert当前的‘ PASSED ’决策更新枚举而不调用reindex。断言响应包括索引模式版本、准备情况、修订和最新的候选决策。

-[] **步骤2：运行测试以验证RED**

```powershell

mvn "-Dmaven.repo.local=.m2/repository" -f services/api/pom.xml "-Dtest=KnowledgeBaseServiceTest,ControllerLayerTest,ConfigAndExceptionTest" test

```

-[] **步骤3：实现服务器授权强制**

冲突异常带有原因代码和消息。‘ GlobalExceptionHandler ’返回409。“KnowledgeBaseService。updateRetrievalProfile ‘调用’ RetrievalProfileGateService '。在保存非经典值之前assertCanActivate。从PATCH控制器中删除P1自动重新索引调用。Import和create路径必须从classic开始；导出的历史概要文件只有在没有门的情况下导入无法激活时才可以保留为元数据。

-[] **步骤4：验证GREEN并提交**

运行service/controller/config tests，然后提交：

```text

feat(rag): enforce default retrieval profile gate

```

任务9:Web Readiness and Gate Comparison UI

* *文件:* *
-修改：‘ services/web/src/types/index.ts ’
-修改：‘ services/web/src/pages/KbDetailPage.tsx ’
-修改：‘ services/web/src/pages/KbDetailPage.test.tsx ’
修改：“services/web/src/components/RagEvalPanel.tsx”
-修改：‘ services/web/src/components/RagEvalPanel.test.tsx ’
修改：‘ services/web/src/api/resources.test.ts ’

-[] **步骤1：编写失败的UI测试

为准备好/未准备好的知识库和通过/阻塞/陈旧的门添加类型化夹具。断言设置显示准备就绪和候选门徽章，当服务器说候选选项不可用时，在本地UI中阻止候选选项，如果状态改变，仍然会显示结构化的409原因。Assert评估比较呈现点击率/引用率/通过率，经典delta和原因文本。

-[] **步骤2：运行测试以验证RED**

从“服务/ web”:

```powershell

npm test -- KbDetailPage RagEvalPanel resources

```

-[] **步骤3：实现最小的操作UI**

用‘ RagEvalProfileMetrics ’， ‘ RagEvalGateDecision ’和gate状态扩展类型。保持现有的紧凑设置和表结构：在配置文件选项旁边添加准备线和状态徽章，然后在现有的评估比较中添加度量/增量列。不要添加调优滑块或解释性功能副本。

-[] **步骤4：验证GREEN并提交**

运行重点Web测试，然后输入‘ npm Run build ’，然后提交：

```text

feat(web): show retrieval profile quality gates

```

任务10：文档和最终顺序验证

* *文件:* *
-修改：‘ README.md ’
-修改：‘ docs/progress.md ’
-修改：‘ docs/todo.md ’
-修改：‘ docs/superpowers/plans/2026-07-18-v1.5-rag-quality-upgrade-implementation.md ’
-修改：‘ docs/superpowers/plans/2026-07-19-v1.5-qa-indexing-implementation.md ’
修改：“deploy/.env.example”

-[] **步骤1：从实际diff**同步文档

记录v2集合、一次性重新索引/割接、概要文件隔离、组合默认值、门需求和409行为。只有在所有验证通过后才将P2待办事项标记为完成。

-[] **第二步：依次执行API验证

设置' $env:MAVEN_OPTS='-Xmx1024m -XX:MaxMetaspaceSize=512m' '，然后运行：

```powershell

mvn "-Dmaven.repo.local=.m2/repository" -f services/api/pom.xml "-Dtest=MilvusVectorServiceTest,KnowledgeBaseServiceTest,RetrievalServiceTest,RagEvalServiceTest,RetrievalProfileGateServiceTest,IngestJobProducerTest,IngestJobServiceTest,DocumentServiceTest,VectorCleanupTaskServiceTest,ControllerLayerTest,ConfigAndExceptionTest,QaGenerationServiceTest" test

```

-[] **步骤3：依次运行Worker验证

```powershell

pytest services/worker/tests/test_main.py services/worker/tests/test_profile_index_plan.py services/worker/tests/test_qa_indexer.py services/worker/tests/test_canonical_parser_consumer.py services/worker/tests/test_parent_child_chunker.py services/worker/tests/test_embedder_hybrid.py services/worker/tests/test_indexer.py -q --basetemp .pytest-tmp/p2-final

```

-[] **步骤4：运行Web验证和生产构建**

从“服务/ web”:

```powershell

npm test -- KbDetailPage RagEvalPanel resources
npm run build

```

-[] **第五步：检查diff和资源

运行:

```powershell

git -c safe.directory='D:/software/cld_project/dupi-RAG/.worktrees/v1.5-rag-quality-upgrade' diff --check
git -c safe.directory='D:/software/cld_project/dupi-RAG/.worktrees/v1.5-rag-quality-upgrade' status --short
Get-Process -Name java,node,python,python3 -ErrorAction SilentlyContinue | Select-Object Id,ProcessName,StartTime,Path

```

期望：没有空白错误，没有生成的缓存/构建输出跟踪，没有Java/Node/Python测试进程启动此工作保留。不要停止Codex桌面运行时或用户拥有的Docker/浏览器进程。

-[] **第六步：提交最终文档**

```text

docs(rag): record v1.5 p2 quality gate completion

```

---

##验证总结

- API测试证明了v2模式/过滤、迁移准备、修订无效、组合向量融合、评估指标、gate强制执行和清理补偿。
- Worker测试证明了通用索引、QA回退、v2标量字段、传统双写、混合过滤和加权路由融合。
Web测试证明gate/readiness状态和概要文件比较指标是可见的，而不会暴露调优控制。
-最终检查是顺序的和资源有限的，因为这个工作站以前在并发构建下显示不稳定。
