# V1.2.1端到端隔离清理实施方案

<!-- language-switch -->
[English](../../../en/superpowers/plans/2026-07-14-v1.2.1-e2e-isolation-cleanup-implementation.md)





> **中文说明：** 本实施计划将浏览器门禁数据迁移到 `e2e` 租户，并清理每次成功运行创建的全部测试资源。

b> **对于代理工作者：**需要的子技能：使用$superpower-subagents（推荐）或$superpower- execution -plans逐个任务地执行此计划。步骤使用复选框（' -[]'）语法通过update_plan进行跟踪。

**目标：**移动真实浏览器网关数据到租户‘ e2e ’，并删除所有成功运行的测试资源。

**架构：**单个“OPS_ADMIN”端点物理上只删除租户恰好为“e2e”的小写“e2e_*”帐户。浏览器端到端创建一个临时的端到端管理员，通过与控制台相同的cookie和CSRF边界切换会话和删除资源。

**技术栈：** Spring Boot， JPA, React/Vitest，剧作家，Docker撰写。

---

##文件结构

- ' services/api/src/main/java/com/dupi/rag/service/AccountService.java '：端到端删除策略。
- ' services/api/src/main/java/com/dupi/rag/controller/OpsController.java '：删除路由和审计操作。
- ' services/api/src/test/java/com/dupi/rag/service/AccountServiceTest.java '：接受和拒绝删除案例。
- ' services/api/src/test/java/com/dupi/rag/controller/ControllerLayerTest.java '：端点委托和审计断言。
——“服务/ web / src / api /知识库。test-account delete wrapper。
——“服务/ web / src / api / resources.test。：包装器路径测试。
——“服务/ web / src /测试/ e2eLifecycle。Ts：完成/失败生命周期决策。
——“服务/ web / src /测试/ e2eLifecycle.test。生命周期助手测试。
——“服务/ web / e2e / browser-gate.spec。隔离的双登录流程和清理。
——“README。医学博士”、“docs / e2e-testing。医学博士”、“docs /进展。医学博士”、“docs /待办事项。Md：操作文件。

##任务1：添加约束后端删除

**Files:**
- Modify: `services/api/src/main/java/com/dupi/rag/service/AccountService.java`
- Modify: `services/api/src/main/java/com/dupi/rag/controller/OpsController.java`
- Modify: `services/api/src/test/java/com/dupi/rag/service/AccountServiceTest.java`
- Modify: `services/api/src/test/java/com/dupi/rag/controller/ControllerLayerTest.java`

- [x] **步骤1：编写失败的测试**

为所需契约添加测试：

```java

service.create(account("e2e_account_42", "e2e", "ANALYST"));
service.deleteE2e("e2e_account_42");
assertThat(stores.users.findByUsername("e2e_account_42")).isEmpty();

assertThatIllegalArgumentException().isThrownBy(() -> service.deleteE2e("analyst"));
assertThatIllegalArgumentException().isThrownBy(() -> service.deleteE2e("e2e_account_default"));

```

在控制器测试中，存根‘ accountService.deleteE2e("e2e_account_42") ’，调用‘ deleteE2eAccount ’，并验证：

```java

verify(auditLogService).recordSuccess(
    eq("ACCOUNT_DELETE_E2E"), eq("ACCOUNT"), isNull(), contains("e2e_account_42"));

```

- [x] **第二步：运行RED**

```powershell

cd services/api
.\mvnw.cmd "-Dtest=AccountServiceTest,ControllerLayerTest" test

```

Expected：由于所需的服务和控制器方法不存在，编译失败。

- [x] **步骤3：执行最小的策略和路由**

将这个方法添加到‘ AccountService ’；不要添加一般帐户删除或更改最后管理规则：

```java

@Transactional
public String deleteE2e(String username) {
    UserAccount user = requireUser(username);
    if (!user.getUsername().startsWith("e2e_") || !"e2e".equals(user.getTenantId())) {
        throw new IllegalArgumentException("only e2e_* accounts in the e2e tenant can be deleted");
    }
    userAccountRepository.delete(user);
    return user.getUsername();
}

```

导入“DeleteMapping”并添加以下控制器动作：

```java

@DeleteMapping("/accounts/{username}")
public void deleteE2eAccount(@PathVariable String username) {
    String deleted = accountService.deleteE2e(username);
    auditLogService.recordSuccess("ACCOUNT_DELETE_E2E", "ACCOUNT", null,
            "Deleted E2E account " + deleted);
}

```

追加‘ ACCOUNT_DELETE_E2E ’到‘ metadata(). auditactions ’。现有的‘ /api/v1/ops/** ’过滤提供了外部‘ OPS_ADMIN ’的要求。

- [x] **第四步：执行“GREEN”命令并提交**

```powershell

cd services/api
.\mvnw.cmd "-Dtest=AccountServiceTest,ControllerLayerTest,ConfigAndExceptionTest" test
git add src/main/java/com/dupi/rag/service/AccountService.java src/main/java/com/dupi/rag/controller/OpsController.java src/test/java/com/dupi/rag/service/AccountServiceTest.java src/test/java/com/dupi/rag/controller/ControllerLayerTest.java
git commit -m "feat(ops): restrict E2E test account deletion"

```

任务2：添加Test-Only Web Wrapper

* *文件:* *
-修改：‘ services/web/src/api/knowledgeBase.ts ’
修改：‘ services/web/src/api/resources.test.ts ’

- [x] **第一步：写RED包装断言**

```ts

await expect(deleteE2eAccount('e2e_account_42')).resolves.toBeUndefined()
expect(apiClient.apiDelete).toHaveBeenCalledWith('/api/v1/ops/accounts/e2e_account_42')

```

运行‘ npm test——src/api/resources.test.ts ’；预期的失败是缺少“deleteE2eAccount”导出。

- [x] **步骤2：实现，验证GREEN，提交**

```ts

export function deleteE2eAccount(username: string): Promise<void> {
  return apiDelete(`${OPS_BASE}/accounts/${username}`)
}

```

运行npm test——src/api/resources.test。然后，承诺：

```powershell

git add src/api/knowledgeBase.ts src/api/resources.test.ts
git commit -m "test(e2e): add isolated account cleanup client"

```

不要在‘ OpsAccountsPage.tsx ’中使用此包装器；范围内没有普通删除按钮。

任务3：隔离并清理浏览器闸门

* *文件:* *
-创建：‘ services/web/src/test/e2eLifecycle.ts ’
-创建：‘ services/web/src/test/e2eLifecycle.test.ts ’
-修改：‘ services/web/e2e/browser-gate. specs .ts ’

- [x] **步骤1：编写并运行RED生命周期测试**

测试必须证明这两个分支：

```ts

await finalizeE2eRun(false, resources, cleanup, attach)
expect(cleanup).not.toHaveBeenCalled()
expect(attach).toHaveBeenCalledWith('e2e-evidence', expect.stringContaining(resources.kbName))

await finalizeE2eRun(true, resources, cleanup, attach)
expect(cleanup).toHaveBeenCalledTimes(1)

```

运行‘ npm test——src/test/e2eLifecycle.test.ts ’；预期的失败是缺少生命周期导出。后续的RED测试还证明，在传播故障之前，清理失败会附加剩余的资源和错误。

- [x] **步骤2：实现和验证绿色生命周期助手**

```ts

export interface E2eRunResources {
  runId: string
  gateUsername: string
  accountUsername: string
  kbName: string
  kbId?: string
}

export async function finalizeE2eRun(completed: boolean, resources: E2eRunResources,
  cleanup: () => Promise<void>, attach: (name: string, body: string) => void | Promise<void>): Promise<void> {
  if (!completed) {
    await attach('e2e-evidence', JSON.stringify(resources, null, 2))
    return
  }
  try {
    await cleanup()
  } catch (error) {
    await attach('e2e-evidence', JSON.stringify({
      ...resources,
      cleanupError: error instanceof Error ? error.message : String(error),
    }, null, 2))
    throw error
  }
}

```

运行‘ npm test——src/test/e2eLifecycle.test.ts ’；产品失败、异步证据附加、成功清理和清理失败案例通过。

- [x] **步骤3：执行双会话剧作家流程**

派生一个运行ID并命名为“e2e_gate_<runId>”、“e2e_account_<runId>”和“e2e-browser-<runId>”。配置的管理员只在角色为ADMIN的租户e2e中创建e2e_gate_<runId>；清除cookie和本地存储密钥“dupi.auth”。Csrf '，然后作为临时管理员登录。

在该端到端会话中运行现有的KB、document/chat/RAG-eval、审计、帐户和角色断言。验证帐户使用租户‘ e2e ’。只有在浏览器最终错误门断言之后才设置‘ completed = true ’。

在“finally”中，调用“finalizeE2eRun”。如果成功，请使用“page”。用浏览器cookie，凭据：‘include’，以及存储的CSRF令牌头按此顺序DELETE：

```ts

`/api/v1/knowledge-bases/${resources.kbId}`
`/api/v1/ops/accounts/${resources.accountUsername}`
`/api/v1/ops/accounts/${resources.gateUsername}`

```

每个清理响应都必须是“ok”；生命周期代码在传播任何清理失败之前附加资源标识符和清理错误。在产品流程失败时，附上名称和“页面”。url() '，不执行DELETE。最后，以配置的管理员身份登录，并断言生成的三个名称不存在。

- [x] **第四步：运行真正的GREEN浏览器并提交**

```powershell

cd ../..
$env:E2E_BASE_URL = 'http://localhost:8080'
$env:E2E_ADMIN_USERNAME = 'admin_fix'
$env:E2E_ADMIN_PASSWORD = '<local-password>'
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/e2e-browser-gate.ps1
git add services/web/src/test/e2eLifecycle.ts services/web/src/test/e2eLifecycle.test.ts services/web/e2e/browser-gate.spec.ts
git commit -m "test(e2e): isolate browser gate data and cleanup"

```

预期：一个Chromium测试通过，配置的管理员之后看不到任何运行的资源。

任务4：记录并验证发布

* *文件:* *
-修改：' README。医学博士”、“docs / e2e-testing。医学博士”、“docs /进展。医学博士”、“docs / todo.md”
-修改：‘ docs/superpowers/plans/2026-07-14-v1.2.1-e2e-isolation-cleanup-implementation.md ’

- [x] **步骤1：同步操作契约**

文档成功清理、失败证据、“DELETE /api/v1/ops/accounts/{username}”上的租户/名称限制，以及故意缺少帐户页面删除按钮。

- [x] **第二步：执行全顺序验证**

```powershell

cd services/api
.\mvnw.cmd verify
cd ../web
npm test
npm run build
cd ../..
$env:E2E_BASE_URL = 'http://localhost:8080'
$env:E2E_ADMIN_USERNAME = 'admin_fix'
$env:E2E_ADMIN_PASSWORD = '<local-password>'
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/e2e-browser-gate.ps1
git diff --check

```

- [x] **第三步：标记完成、评审、提交、推送

使用实际的浏览器时间更新V1.2.1进度/待办事项记录，标记每个已完成的复选框，检查阶段性差异和秘密，然后提交“feat(V1.2.1)：隔离和清理浏览器测试数据”并推送分支。

# #自我回顾

—规格覆盖：任务1-3实现限制删除、端到端租户隔离、成功清理和失败证据；任务4涵盖文档和验证。
-占位符扫描：‘ <local-password> ’是仅运行时的，永远不能提交。
-类型一致性：后端‘ deleteE2e ’；控制器/ Web“deleteE2eAccount”;生命周期字段‘ gateUsername ’， ‘ accountUsername ’， ‘ kbName ’和‘ kbId ’。
