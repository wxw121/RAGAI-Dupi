# V1.5 Main Conflict Recovery Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use $superpower-subagents (recommended) or $superpower-executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking via update_plan.

**Goal:** Recreate the V1.5-to-main merge without committed conflict markers while preserving both V1.4.2 governance/recovery behavior and V1.5 retrieval-quality behavior.

**Architecture:** Start from the verified V1.5 commit `8baa137`, merge `origin/main` at `3296a87`, and resolve conflicts by domain rather than choosing one side wholesale. Treat `main` as authoritative for governance, recovery, upload controls, and sparse retrieval foundations; layer V1.5 retrieval profiles, parent-child/QA indexing, provenance, and evaluation gates onto those contracts.

**Tech Stack:** Java 17/Spring Boot/Maven, Python/pytest, React/TypeScript/Vitest/Vite, Git.

---

### Task 1: Reproduce the bad merge safely

**Files:**
- Inspect: all files reported by `git diff --name-only --diff-filter=U`
- Test: repository-wide conflict-marker scan

- [ ] **Step 1: Confirm the recovery base**

Run:

```powershell
git status --short --branch
git rev-parse HEAD origin/main
```

Expected: clean `codex/v1.5-rag-quality-upgrade` at `8baa137`, with `origin/main` at `3296a87`.

- [ ] **Step 2: Merge main without committing**

Run:

```powershell
git merge --no-commit --no-ff origin/main
```

Expected: merge stops with conflicts; no merge commit is created.

- [ ] **Step 3: Capture the failing proof**

Run:

```powershell
git diff --name-only --diff-filter=U
rg -n "^(<<<<<<<|=======|>>>>>>>)" services
```

Expected: the conflict list is non-empty and marker scan fails the release condition.

### Task 2: Resolve API contracts and persistence

**Files:**
- Modify: `services/api/src/main/java/com/dupi/rag/config/ApiKeyAuthFilter.java`
- Modify: `services/api/src/main/java/com/dupi/rag/controller/KnowledgeBaseController.java`
- Modify: `services/api/src/main/java/com/dupi/rag/domain/entity/KnowledgeBase.java`
- Modify: `services/api/src/main/java/com/dupi/rag/domain/entity/RagEvalRun.java`
- Modify: `services/api/src/main/java/com/dupi/rag/domain/entity/RagEvalRunResult.java`
- Modify: `services/api/src/main/java/com/dupi/rag/dto/IngestJobMessage.java`
- Modify: `services/api/src/main/java/com/dupi/rag/dto/RagEvalRunRequest.java`
- Modify: `services/api/src/main/java/com/dupi/rag/dto/RagEvalRunResponse.java`
- Modify: `services/api/src/main/java/com/dupi/rag/dto/RagEvalRunResultResponse.java`
- Modify: `services/api/src/main/java/com/dupi/rag/repository/KnowledgeBaseRepository.java`
- Modify: `services/api/src/main/java/com/dupi/rag/repository/RagEvalRunRepository.java`

- [ ] **Step 1: Merge fields and signatures**

Preserve main's lifecycle, governance, recovery, sparse-migration, and rank-evidence fields. Preserve V1.5's retrieval-profile, index-schema, profile comparison, gate-decision, and evaluation-revision fields. Resolve duplicate imports and ensure constructor/call-site signatures match the combined records.

- [ ] **Step 2: Compile API contracts**

Run:

```powershell
mvn "-Dmaven.repo.local=.m2/repository" -f services/api/pom.xml -DskipTests compile
```

Expected: compilation advances past entity/DTO/repository syntax and type checks.

### Task 3: Resolve API services and tests

**Files:**
- Modify: `services/api/src/main/java/com/dupi/rag/service/DocumentService.java`
- Modify: `services/api/src/main/java/com/dupi/rag/service/IngestJobProducer.java`
- Modify: `services/api/src/main/java/com/dupi/rag/service/IngestJobService.java`
- Modify: `services/api/src/main/java/com/dupi/rag/service/RagEvalService.java`
- Modify: `services/api/src/main/java/com/dupi/rag/service/RetrievalService.java`
- Modify: corresponding conflicted API tests

- [ ] **Step 1: Compose service behavior**

Keep main's governance callbacks, recovery state, sparse retrieval, reranking, and upload accounting. Retain V1.5 profile readiness, QA/parent provenance, weighted fusion, evaluation gates, cutover invalidation, and top-K limits. Update tests to assert the combined behavior rather than deleting either side's cases.

- [ ] **Step 2: Run API tests**

Run:

```powershell
mvn "-Dmaven.repo.local=.m2/repository" -f services/api/pom.xml test
```

Expected: all API tests pass with zero failures and errors.

### Task 4: Resolve Worker behavior and tests

**Files:**
- Modify: `services/worker/app/config.py`
- Modify: `services/worker/app/consumer.py`
- Modify: `services/worker/app/indexer.py`
- Modify: `services/worker/app/main.py`
- Modify: `services/worker/app/retrieval/hybrid.py`
- Modify: corresponding conflicted Worker tests

- [ ] **Step 1: Compose indexing and retrieval paths**

Preserve main's reliable queue, governance callbacks, sparse index/retrieval, and reranker contracts. Retain V1.5 parent-child and QA index plans, schema-v2 metadata, provenance propagation, and profile-specific retrieval behavior.

- [ ] **Step 2: Run Worker tests**

Run:

```powershell
pytest services/worker/tests -q --basetemp .pytest-v15-merge -p no:cacheprovider
```

Expected: all Worker tests pass.

### Task 5: Resolve Web contracts and UI

**Files:**
- Modify: `services/web/src/api/knowledgeBase.ts`
- Modify: `services/web/src/components/RagEvalPanel.tsx`
- Modify: `services/web/src/pages/KbDetailPage.tsx`
- Modify: `services/web/src/types/index.ts`

- [ ] **Step 1: Compose API types and panels**

Keep main's governance, recovery, sparse migration, and upload UI contracts. Retain V1.5 retrieval-profile selection and evaluation comparison/gate presentation. Remove duplicate imports and reconcile shared response types.

- [ ] **Step 2: Run Web tests and build**

Run:

```powershell
Set-Location services/web
npm test
npm run build
```

Expected: all Web tests pass and Vite completes the production build.

### Task 6: Verify and commit the repaired merge

**Files:**
- Verify: entire merge result

- [ ] **Step 1: Prove all conflicts are resolved**

Run:

```powershell
git diff --name-only --diff-filter=U
rg -n "^(<<<<<<<|=======|>>>>>>>)" services
git diff --check
```

Expected: no unmerged paths, no conflict-marker matches, and no whitespace errors.

- [ ] **Step 2: Review the combined diff**

Compare the merge result against both parents and review the API, Worker, Web, migrations, and documentation for lost behavior or duplicate contracts.

- [ ] **Step 3: Create the merge commit**

Run:

```powershell
git add <each-reviewed-file>
git commit -m "merge: reconcile main with v1.5 rag quality upgrade"
```

Expected: one two-parent merge commit with no conflict markers and a clean worktree after temporary artifacts are removed.

## Verification

- API full test suite passes.
- Worker full test suite passes.
- Web full test suite passes.
- Web production build passes.
- `git diff --name-only --diff-filter=U` is empty.
- Conflict-marker scan is empty.
- `git diff --check` exits zero.
- Final code review reports no Critical or Important findings.

## Next skill

Use `$superpower-executing-plans` for inline execution, followed by `$superpower-review` and `$superpower-verification`.
