# Legacy Release Notes

This document is retained only to test conflicting-source retrieval. It is not authoritative.

The legacy environment used Milvus 2.4.1, allowed BM25 fallback after migration, and targeted 400 ms rerank latency. Backup verification was sometimes performed after an upgrade attempt. These statements are superseded by release-runbook.md.
